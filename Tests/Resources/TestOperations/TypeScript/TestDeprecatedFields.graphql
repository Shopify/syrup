query TestDeprecatedFields($first: Int) {
  channels(first: $first) {
    edges {
      node{
        app {
          id
          icon{
            src
          }
        }
        
      }
    }
  }
}
